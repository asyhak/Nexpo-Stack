// UniWind theming is CSS-variable based, defined in global.css via @variant directives.
// No runtime JS config is needed here — theme values live in the app's global.css.
export const config = {};
export const colors = {};
